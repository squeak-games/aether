package com.squeakgames.aether.audio

class AetherAudioEngine {
    // Oboe JNI bridge entry point — native audio pipeline will be loaded
    // from src/main/cpp/ via System.loadLibrary("aether-audio").
}
